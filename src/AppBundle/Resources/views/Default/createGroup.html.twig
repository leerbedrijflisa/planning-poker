{% extends '::base.html.twig' %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}


{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_rest(form) }}
    <button type="submit" class="btn btn-success btn-block">Aanmaken</button>
    {{ form_end(form) }}
{% endblock %}